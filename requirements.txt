# ============================================================
# Training / cloud GPU deps (Colab / Kaggle T4 — CUDA required)
# Install on the GPU box, NOT the Mac.
#   pip install -r requirements.txt
# ============================================================

# Fast QLoRA + DPO on a single GPU. unsloth pins compatible
# transformers/peft/trl/bitsandbytes versions for you.
unsloth
unsloth_zoo

# Core HF stack (unsloth will align exact versions)
transformers>=4.46.0
datasets>=3.0.0
trl>=0.12.0
peft>=0.13.0
accelerate>=1.0.0
bitsandbytes>=0.44.0

# Data / eval / serving
pyyaml>=6.0
python-dotenv>=1.0.0
rouge-score>=0.1.2
sentencepiece>=0.2.0
gradio>=4.44.0

# RAG baseline + comparison benchmark
sentence-transformers>=3.0.0,<5.0
faiss-cpu>=1.8.0
matplotlib>=3.8.0

# Cloud API serving (CPU GGUF)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
llama-cpp-python>=0.3.0

# Cloud backup of artifacts (hub uses huggingface_hub, already pulled by transformers)
# google-cloud-storage>=2.18.0   # uncomment only if you back up to a GCS bucket

# Tests
pytest>=8.0

# Optional: experiment tracking + LLM-as-judge
wandb>=0.18.0
groq>=0.11.0           # free judge provider (default)
# google-generativeai>=0.8.0   # uncomment for the Gemini judge
