# ============================================================
# Local Mac (Apple Silicon M2) deps — data prep + inference only.
# NO bitsandbytes / no CUDA training here.
#   source venv/bin/activate
#   pip install -r requirements-mac.txt
# ============================================================

# Data prep (CPU)
datasets>=3.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
sentencepiece>=0.2.0

# Native Apple-Silicon inference
mlx-lm>=0.19.0

# Browser demo + eval
gradio>=4.44.0
rouge-score>=0.1.2

# RAG index build + benchmark (CPU on Mac)
sentence-transformers>=3.0.0,<5.0
faiss-cpu>=1.8.0
matplotlib>=3.8.0

# Cloud-style API locally (CPU GGUF)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
llama-cpp-python>=0.3.0

# Tests
pytest>=8.0

# Optional LLM-as-judge (free Groq default; others optional)
groq>=0.11.0
# google-generativeai>=0.8.0   # uncomment for the Gemini judge
