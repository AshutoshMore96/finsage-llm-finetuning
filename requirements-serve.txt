# ============================================================
# Serving deps for the FastAPI + llama.cpp API (CPU, GGUF).
# Works on a cheap cloud VM, container, or the Mac.
#   pip install -r requirements-serve.txt
# ============================================================
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
llama-cpp-python>=0.3.0
pyyaml>=6.0
