# All deps resolve to musllinux wheels on Alpine (no compiling):
#  - uvicorn (plain, not [standard]) avoids uvloop/httptools source builds
#  - huggingface_hub pinned below the hf-xet (rust) dependency
#  - llama-cpp-python prebuilt musl CPU wheel from the index
fastapi
uvicorn
huggingface_hub==0.25.2
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python==0.3.19
