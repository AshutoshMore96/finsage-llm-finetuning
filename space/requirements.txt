# llama-cpp-python unpinned -> builds the latest llama.cpp from source, which (unlike the
# old prebuilt wheels) decodes this Qwen2.5 GGUF correctly. Compiled glibc-native here.
fastapi
uvicorn
huggingface_hub
llama-cpp-python
