fastapi
uvicorn[standard]
# Prebuilt CPU wheel, version-pinned to one that EXISTS on the index, so pip installs
# the wheel instead of compiling from source (unpinned grabs a newer PyPI sdist -> OOM build).
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python==0.3.19
huggingface_hub
